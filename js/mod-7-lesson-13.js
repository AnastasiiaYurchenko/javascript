// const imageContainer = document.querySelector('.images-gallery')

// const imgElements =
// images.map(item) => {
//     imageContainer.insertAdjacentHTML('beforeend',
//     '<img src="${item.src}" title="${item.title}" alt="${item.title}"/>')
// }